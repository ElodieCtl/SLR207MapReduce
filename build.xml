<project name="SLR207Project" default="compile" basedir=".">
  <description>
    Project which implements a Map Reduce
  </description>
  <!-- set global properties for this build -->
  <property name="src" location="src"/>
  <property name="build" location="build"/>

  <target name="init">
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="${build}"/>
  </target>

  <target name="compile" depends="init"
        description="compile the source">
    <!-- Compile the Java code from ${src} into ${build} -->
    <javac srcdir="${src}" destdir="${build}"/>
  </target>

  <target name="run-sequential" depends="compile"
        description="run the sequential project">
    <!-- Execute the Java code from ${build} -->
    <java classname="src.SequentialCounter" classpath="${build}"/>
  </target>

  <target name="run-master" depends="compile"
        description="run the master">
    <!-- Execute the Java code from ${build} -->
    <java classname="src.Master" classpath="${build}"/>
  </target>

  <target name="run-slave" depends="compile"
        description="run the slave">
    <!-- Execute the Java code from ${build} -->
    <java classname="src.Slave" classpath="${build}"/>
  </target>

  <target name="clean"
        description="clean up">
    <!-- Delete the ${build} directory trees -->
    <delete dir="${build}"/>
  </target>
</project>